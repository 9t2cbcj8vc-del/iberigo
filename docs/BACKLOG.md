# IberiGo Backlog

This is the internal product backlog for IberiGo.

## In Progress

- **Group 1 visual QA before publication** (from Sprint 65 human review — content acceptable for now, visual/design work required before any Group 1 page publishes):
  - review page layout
  - check mobile layout
  - check desktop layout
  - check spacing and card design
  - check typography hierarchy
  - check CTA presentation
  - check consistency across Group 1
  - only after visual QA, reconsider publication readiness
  - ~~open question from the reviewer: which visual/design system should Group 1 use before publication?~~ **Answered (Sprint 66): IberiGo Guide System v1** — calm, practical, government-adjacent, card-based, mobile-first; see `docs/IBERIGO_GUIDE_SYSTEM_V1.md`
  - Guide System v1 CSS scope audit completed (Sprint 67) — recommended approach: scoped inline `guideCss()` styles (Option D), zero exposure to live indexed pages; unsafe selectors documented; see `docs/GUIDE_SYSTEM_V1_CSS_SCOPE_PLAN.md`
  - ~~Next step: apply scoped Guide System v1 styles to Group 1~~ **Applied (Sprint 68)** — scoped inline `guideCss()` pass, styles.css untouched, scope check passed; browser QA at 390px/1280px found no overflow, 44px tap targets, stacking tables, calm warning styling (details in `docs/GROUP1_REVIEW_PACKAGE.md`)
  - Local visual QA pack prepared (Sprint 69) — screenshots in `visual-qa/group1/` (untracked), report in `docs/GROUP1_VISUAL_QA_REPORT.md`; zero visual issues, one content finding
  - User-facing source-note cleanup completed (Sprint 70) — internal verification commentary removed from all official-source card notes and Source status banners across every generated page; verification history preserved in `docs/SOURCE_VERIFICATION_MATRIX.md`
  - Human visual review found TOC overlap/cramped sidebar and reading-time placement issues (Sprint 72); scoped fixes applied in generated guide components only, with `styles.css` untouched
  - Cross-page Guide System v1 consistency QA completed (Sprint 73) — all 22 draft/noindex surfaces checked at desktop/mobile widths; no new visual consistency issues found; details in `docs/GUIDE_SYSTEM_V1_CONSISTENCY_QA.md`
  - **Status: Visual review completed — still not published** — no page approved for publication, all pages remain noindex, nofollow; legal/tax/immigration and final editorial approval remain separate publish blockers
- Group 1 legal review brief created (Sprint 77) — reviewer questions, source summaries, source limitations, and allowed review outcome fields are in `docs/GROUP1_LEGAL_REVIEW_BRIEF.md`; Group 1 still not published, no page approved for indexing, all pages remain noindex, nofollow
- Group 1 owner-review conservative risk pass completed (Sprint 80) — no external legal/professional review is available yet; wording was checked and lightly softened for obvious risk, the shared disclaimer was broadened, and Group 1 remains not published with all pages still noindex, nofollow. Status: **Owner review risk pass completed — still not published**.
- Group 1 owner risk pass preview QA completed (Sprint 82) — PR #14 preview checked at desktop and mobile widths; the new trust/disclaimer blocks are calm, appear once per page, and do not create overflow, TOC, reading-time, card or CTA regressions. Status: **Owner risk pass preview QA completed — still not published**.
- Group 1 publication decision package created (Sprint 84) — decision record and future technical launch checklist are in `docs/GROUP1_PUBLICATION_DECISION.md` and `docs/GROUP1_TECHNICAL_LAUNCH_CHECKLIST.md`; owner publication decision is pending, Group 1 is still not published, no page is approved for indexing, and all pages remain noindex, nofollow. Status: **Eligible for owner publication decision — still not published**.
- Group 1 owner publication choice recorded (Sprint 87) — owner has chosen to proceed toward publishing all five Group 1 pages (`/start-here/`, `/moving-to-spain/documents-checklist/`, `/moving-to-spain/finding-accommodation/`, `/moving-to-spain/settling-into-spain/`, `/living-in-spain/opening-a-bank-account/`) as owner-reviewed practical information, since no external legal/professional review is available; this is not legal, tax, immigration, financial or rental advice. Technical launch has not happened — all pages remain noindex, nofollow, sitemap/robots/redirects/homepage/navigation are unchanged, and no PR or merge has been done. Status: **Owner publication selected — technical launch pending**. See `docs/GROUP1_PUBLICATION_DECISION.md` and `docs/GROUP1_TECHNICAL_LAUNCH_CHECKLIST.md`.
- Group 1 technical launch PR prepared (Sprint 88) — `noindex, nofollow` removed only from the five selected pages, five pages added to `sitemap.xml`, `robots.txt` left unchanged (already permitted the routes), no redirects added, no legacy guide migrated; all 16 other draft pages verified still `noindex, nofollow`; `/guides/banking/` and `/guides/eu-registration/` confirmed still returning `200`. No external legal/professional review was available; launch remains owner-reviewed practical information. PR opened into `main`, not merged. Status: **Technical launch PR prepared — preview review pending**.
- Group 1 launch preview QA passed (Sprint 89) — PR #16 (commit `7ff11a4`) checked on its Netlify deploy preview: all five selected pages return `200` with no DRAFT badge, no `noindex`/`nofollow`, correct canonical/title/description/Open Graph, visible disclaimer, working CTAs, and no layout break at 1280px/390px; the preview's `sitemap.xml` and `search-index.json` each contain exactly the five selected pages; nine representative non-selected draft pages confirmed still `noindex, nofollow` and absent from the sitemap; `robots.txt` unchanged; `/guides/banking/` and `/guides/eu-registration/` still return `200`; no redirects or legacy migrations made. No issues found, no fixes needed. Future recommendation (not actioned): add a homepage/navigation link to `/start-here/`. Status: **Launch preview QA passed — merge pending**. PR #16 remains unmerged.
- **Group 1 launched (Sprint 90, 2026-07-03)** — PR #16 squash-merged into `main` (commit `1dc6b9b`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification on `https://iberigo.eu` confirmed the five selected pages (`/start-here/`, `/moving-to-spain/documents-checklist/`, `/moving-to-spain/finding-accommodation/`, `/moving-to-spain/settling-into-spain/`, `/living-in-spain/opening-a-bank-account/`) return `200`, `index, follow`, no DRAFT badge, correct canonical/title/description, visible disclaimer; production `sitemap.xml` and `search-index.json` contain exactly these five pages; eight representative non-selected drafts remain `noindex, nofollow` and out of the sitemap; `robots.txt` unchanged; `/guides/banking/` and `/guides/eu-registration/` return `200`; no redirects, legacy migration, or homepage/navigation changes made. Status: **Group 1 launched as owner-reviewed practical information**. Not legal, tax, immigration, financial or rental advice; not professional approval. Future recommendation, not actioned: add a homepage or navigation link to `/start-here/`.
- **Homepage discovery link prepared (Sprint 92)** — a small link to `/start-here/` (with calm supporting copy, no legal/professional-advice framing) added to the homepage hero on `group1/post-launch-homepage-link`, using the existing `.secondary-action` style, no `styles.css` changes. No additional page published: the five launched pages remain `index, follow`, non-selected drafts remain `noindex, nofollow`, `sitemap.xml`/`search-index.json`/`robots.txt` unchanged, no redirects or legacy migration. Status: **Homepage discovery link prepared — preview review pending**. Not yet merged.
- **Homepage discovery link preview QA passed (Sprint 93)** — PR #18 (commit `7ed2612`) checked on its Netlify deploy preview at 1280px and 390px: homepage returns `200`, `/start-here/` link visible and naturally placed under the hero cards, calm copy, no crowding, no legal/professional-advice implication, no overflow or style regression; `/start-here/` returns `200` and remains `index, follow`; all five launched pages remain `index, follow`; eight representative non-selected drafts remain `noindex, nofollow`; preview's `sitemap.xml`/`search-index.json`/`robots.txt`/`styles.css` byte-identical to `main`; no redirects or legacy migration; `/guides/banking/` and `/guides/eu-registration/` return `200`. No issues found, no fixes needed. Status: **Homepage discovery link preview QA passed — merge pending**. PR #18 remains unmerged.
- **Move to Spain card link prepared (Sprint 94 revision)** — PR #18's standalone "Start here" block was merged and is live in production; this revises it: the block is removed from `index.html`, and the existing "Move to Spain" homepage card's "Explore" button now links directly to `/start-here/` via the existing `.primary-action` style. "Vacation" and "Live" cards unchanged; no global navigation change. No additional page published: five launched pages remain `index, follow`, non-selected drafts remain `noindex, nofollow`, `sitemap.xml`/`search-index.json`/`robots.txt`/`styles.css` unchanged (byte-identical to `main`), no redirects or legacy migration. Status: **Homepage Move to Spain link preview QA pending**. Prepared on a new PR, not yet merged.
- **Sitewide visual coherence audit completed (Sprint 95A)** — paused the PR #19 merge decision (PR #19 revises the homepage "Move to Spain" card to link to `/start-here/`, replacing the standalone block from Sprint 92/93) to audit whether the whole public site (homepage, Vacation/Live sections, The Spain Files, Donate, legacy `/guides/*`, and the launched Guide System v1 pages) feels coherent. Findings: three distinct visual systems exist today (original/homepage, The Spain Files' own magazine style, Guide System v1); the most significant gap is structural, not stylistic — legacy `/guides/*` pages have no static per-guide layout at all (they're homepage clones populated client-side via `data-guide-id`), not a color/radius mismatch. Recommendation: **merge PR #19 as-is**; none of the findings should block it. Full inventory, comparison, risk ranking, and a staged (not global) unification plan are in `docs/SITEWIDE_VISUAL_COHERENCE_AUDIT.md`. Audit only — no implementation, no `styles.css` edits, no publication or indexing changes, no redirects, no legacy migration. Status: **Sitewide visual coherence audit completed — implementation pending**. PR #19 remains open, unmerged, and not closed.
- **Guide System card CTA alignment fix prepared (Sprint 97A)** — found that `.guide-info-card` (used for "Choose your path" and all other Guide System card grids, incl. journey-group "Previous/Next Step"/"Related Guides" cards) had no flex layout, so CTA buttons sat wherever the card's text happened to end, with intrinsic (label-length-dependent) widths — the reported "uneven CTA" bug. Fixed by making `.guide-info-card` a `flex-direction: column` container with `.guide-info-card > .guide-button { margin-top: auto; width: 100%; }`, scoped entirely inside `scripts/guide-components.js`'s `guideCss()` (no `styles.css` edit). Verified on `/start-here/`'s 7-card "Choose your path" grid and the densest draft page (`/moving-to-spain/non-eu-citizens/`, 8-card grid): all buttons in a row now share identical width and bottom offset, min-height stays ≥44px, long labels wrap to two lines cleanly, no horizontal overflow at 1280px/390px, TOC/reading-time/disclaimer unaffected. No additional pages published, no indexing changes, no redirects, no legacy migration, `sitemap.xml`/`search-index.json`/`robots.txt`/homepage/`styles.css` all unchanged. Status: **Guide card CTA alignment fix prepared — preview QA pending**.
- **Guide card CTA alignment fix revised (Sprint 97B)** — the PR #21 CSS-only fix was not enough: even with the flex/bottom-align CSS, cards in the same row still looked uneven because CTA labels varied wildly in length (24–49 characters), causing some buttons to wrap to two lines (82px tall) next to single-line 62px buttons. Fixed by normalizing CTA labels in the two affected route-card grids: `/start-here/`'s "Choose your path" (7 cards) and `/moving-to-spain/non-eu-citizens/`'s "Choose your route" (8 cards) now use a single consistent label, "View roadmap" (with "Continue" for the one card in the non-EU grid that links to Start Here rather than a roadmap). Also found and fixed the same problem sitewide in every page's "Related Guides"/"Previous Step"/"Next Step" journey cards, which pull labels from the shared `guideSummaries` object (`scripts/generate-guide-system.js`) — normalized those to "View roadmap" for roadmap-titled entries and "View guide" for guide-titled entries, and updated the `guideLink()`/`GuideLinkCard()` fallback defaults to match. All changes are in `scripts/generate-guide-system.js` and `scripts/guide-components.js` only — no `styles.css` edit. Verified: every button in every checked grid (`/start-here/`, `/moving-to-spain/non-eu-citizens/`, `/moving-to-spain/family-member-eu-citizen/`, `/moving-to-spain/work-in-spain/`, `/moving-to-spain/settling-into-spain/`) now shares one distinct width and one distinct height (44px, no more wrapped outliers) per grid, no horizontal overflow at 1280px/390px, TOC/reading-time/disclaimer unaffected. No additional pages published, no indexing changes, no redirects, no legacy migration, `sitemap.xml`/`search-index.json`/`robots.txt`/homepage/`styles.css` unchanged. Status: **Guide card CTA alignment fix revised — preview QA pending**.
- **Guide card CTA alignment preview QA passed (Sprint 97C)** — PR #21 (commit `d56bfcc`) checked on its Netlify deploy preview (`https://deploy-preview-21--iberigo.netlify.app`) at 1280px and 390px. `/start-here/`'s 7 "Choose your path" cards all show "View roadmap" with identical 197×44px buttons, centered text, consistent bottom alignment, no wrapping, no overflow. Secondary pages confirmed coherent: `/moving-to-spain/non-eu-citizens/` (8-card grid, 7× "View roadmap" + 1× "Continue" for the Start-Here card, plus a 3-card "View guide" related-guides grid), `/moving-to-spain/family-member-eu-citizen/`, `/moving-to-spain/work-in-spain/`, and `/moving-to-spain/settling-into-spain/` all show uniform 44px-height buttons with no wrapped 82px outliers; `/living-in-spain/opening-a-bank-account/` has no CTA-card grids and was unaffected. TOC, reading-time, and disclaimer confirmed present on all checked pages; all 44px+ touch targets on the densest mobile page (11 buttons). Launch safety: all 5 launched pages remain `index, follow`; sampled drafts remain `noindex, nofollow`; preview's `sitemap.xml`, `search-index.json`, `robots.txt`, `styles.css` byte-identical to `main` (homepage diff limited to Netlify's own preview-injection script and standard pretty-URL rewriting, not a source change); `/guides/banking/` and `/guides/eu-registration/` both return `200`. No issues found, no fixes needed. Status: **Guide card CTA alignment preview QA passed — merge pending**. PR #21 remains unmerged.
- **Guide and homepage CTA alignment (Sprint 97D)** — added a homepage CTA fix to PR #21 before merge. Context: production `/start-here/` still shows the old, uneven "View the X Roadmap" buttons because PR #21 is not merged yet (confirmed: PR #21's preview shows 7× "View roadmap," production shows the old long labels); separately, after PR #19 the live homepage's "Move to Spain" card (now an `<a class="primary-action">`) rendered visibly larger than the "Vacation"/"Live" cards' `<button>` CTAs. Root cause: `.situation-card--illustrated button` (styles.css) is a specific illustrated-card-only rule that didn't match the new anchor, so it fell back to the larger generic `.primary-action` rule; a second, later, equal-specificity rule (`.situation-card button, .popular-card button, .choice-actions button { min-width: 144px; ... }`) also overrode `<button>`-only sizing, and native `<button>` `appearance: auto` added its own UA sizing. Fixed with a small, scoped `styles.css` change: extended `.situation-card--illustrated button`'s rule to also match `.situation-card--illustrated .primary-action`, reset `appearance: none` on both, and added a targeted override (`min-width: 136px; margin-top: clamp(...)`) placed after the competing generic rule so it wins the cascade for illustrated cards only — deliberately not touching `width` so the existing mobile `width: 100%` rule for `.primary-action`/`button` still applies untouched. Verified all three homepage Explore buttons now render identically (136×44px desktop, full-width 44px-tall on mobile, same vertical position) while Move to Spain still links to `/start-here/` and Vacation/Live route-preset behavior is unchanged; no standalone Start Here block returned. Re-verified Guide System pages (`/start-here/`, `/moving-to-spain/non-eu-citizens/`, `/moving-to-spain/family-member-eu-citizen/`, `/moving-to-spain/work-in-spain/`, `/moving-to-spain/settling-into-spain/`) are unaffected by the `styles.css` change — all CTA grids remain uniform. No additional pages published, no indexing changes, no redirects, no legacy migration, `sitemap.xml`/`search-index.json`/`robots.txt` unchanged; `/guides/banking/` and `/guides/eu-registration/` both return `200`. Status: **Guide and homepage CTA alignment preview QA pending**. PR #21 remains unmerged.
- **CTA alignment final preview QA passed (Sprint 97E)** — PR #21 (commit `f1851d7`) checked on its Netlify deploy preview (`https://deploy-preview-21--iberigo.netlify.app`) at 1280px and 390px. Confirmed the `styles.css` diff is narrowly scoped to `.situation-card--illustrated` selectors only (verified via diff against `main`). Homepage: all three Explore buttons (Move to Spain, Vacation, Live) render identically — 136×44px at 1280px, full-width 44px at 390px, same vertical position; Move to Spain still links to `/start-here/`; Vacation/Live route-preset behavior unchanged; no standalone Start Here block; header, search icon, language switcher, footer and Donate link all normal; no overflow. Guide System: `/start-here/` (7× "View roadmap"), `/moving-to-spain/non-eu-citizens/` (8-card grid: 7× "View roadmap" + 1× "Continue", plus 3-card "View guide" related grid), `/moving-to-spain/family-member-eu-citizen/`, `/moving-to-spain/work-in-spain/`, `/moving-to-spain/settling-into-spain/` all show uniform 44px-height buttons, no wrapped 82px outliers, no overflow; `/living-in-spain/opening-a-bank-account/` has no CTA-card grids and is unaffected; TOC/reading-time/disclaimer confirmed present. Regression check on non-guide pages since `styles.css` changed: `/the-spain-files/`, `/support/` (Donate), `/guides/banking/`, `/guides/eu-registration/` all render normally with no button-size or layout regression (none of them use `.situation-card--illustrated`). Launch safety: all 5 launched pages remain `index, follow`; sampled drafts remain `noindex, nofollow`; preview's `sitemap.xml`/`search-index.json`/`robots.txt` byte-identical to `main`; `/guides/banking/` and `/guides/eu-registration/` both return `200`. **Production `/start-here/` still shows the old, un-normalized buttons — this only changes once PR #21 merges.** No issues found, no fixes needed. Status: **CTA alignment final preview QA passed — merge pending**. PR #21 remains unmerged.
- **User-facing Draft labels removed (Sprint 97E)** — added to PR #21 before merge. Found a visible "DRAFT — Not reviewed for publication" badge rendered by `StatusBadge()` in `scripts/guide-components.js` on all 16 non-selected draft pages (the 5 launched pages already showed nothing, since `status: "published"` was never in the badge's label map). Also found a dormant, unused fallback in `GuideHero()`'s default parameters (`asideTitle = "Draft guide"`, `asideText = "...will be reviewed before publication."`) that could leak workflow language if a future page call ever omitted `asideTitle`. Fixed: `StatusBadge()` now always returns an empty string (workflow status stays internal, drives `robots`/search-index logic only, never renders); removed the now-unused `.guide-status-badge` CSS; replaced `GuideHero`'s default fallback text with calm, non-workflow copy ("About this guide" / "This page is part of the IberiGo guide system."). The internal, non-rendered `<script type="application/json" class="guide-frontmatter">` block still legitimately contains `"status": "draft"` per page — left untouched, as instructed, since it's never shown to visitors and is required for generation/validation. `robots` meta logic in `GuideLayout` was not touched. Verified after regeneration: zero visible `.guide-status-badge` elements remain across all 21 generated pages; only the internal JSON frontmatter still says `"status": "draft"` (1 occurrence per non-selected page, non-visible). Visual QA on `/moving-to-spain/non-eu-citizens/`, `/living-in-spain/taxes/`, `/search/`, `/start-here/` at 1280px/390px: no badge, no awkward empty space, reading-time and hero balance unaffected, CTA alignment/label fixes and homepage Move/Vacation/Live consistency both still hold, no overflow. Launch safety unchanged: 5 launched pages remain `index, follow`, 16 drafts remain `noindex, nofollow`, `sitemap.xml`/`search-index.json`/`robots.txt` unchanged, `/guides/banking/` and `/guides/eu-registration/` return `200`. Status: **User-facing Draft labels removed — final preview QA pending**. PR #21 remains unmerged.
- **Guide source-card styling restored (Sprint 97F)** — added to PR #21 before merge. Audited the legacy/live source-link styling: `app.js`'s wizard-result flow already has a mature, safe source-category system (`.gov-link`/`.gov-link-badge`/`.gov-link-bars`/`.gov-link-stars` in `styles.css`, driven by `govMeta`/`govDomains`/`euDomains` in `app.js`) with 5 existing variants (general, fee, social, health, eu) using accent colors and small badge icons — no logos, no literal flags, already vetted safe. The new Guide System's `OfficialSources()` cards had none of this — every official source rendered as a plain, undifferentiated `.guide-info-card`. Implemented a parallel, scoped variant system entirely in `scripts/guide-components.js` (new `classifySource()` + `SOURCE_CATEGORY_META`, new CSS confined to `guideCss()`, no `styles.css` edit): 9 categories — `government`, `police`, `eu`, `tax`, `social-security`, `traffic`, `healthcare`, `municipal`, `generic` — each with a distinct accent color (left border + small 2-4 letter initials badge, e.g. "PN" for Policía Nacional, "DGT" for traffic, "SS" for Social Security) and an uppercase category tag ("POLICÍA NACIONAL", "TRAFFIC AUTHORITY", etc.), auto-detected from each source's URL domain (reusing the same domain list logic as `app.js`) or, for sources with no single URL (e.g. "Local Town Halls"), from the source name. Verified classification is correct across `/moving-to-spain/eu-citizens/` (government + police + municipal), `/moving-to-spain/eu-registration/`, `/moving-to-spain/family-member-eu-citizen/` (adds EU blue), `/living-in-spain/taxes/` (tax gold), `/living-in-spain/driving/` (traffic orange), `/moving-to-spain/healthcare/` (adds healthcare teal + social-security blue + municipal), `/moving-to-spain/documents-checklist/`. Visual QA at 1280px/390px: cards feel coherent and trustworthy without impersonating official sites, consistent height/spacing/icon area preserved, no horizontal overflow, mobile layout clean. Confirmed no regression: Guide card CTA normalization/alignment, homepage Move/Vacation/Live CTA consistency, and the Draft-label removal from Sprint 97E all still hold. No additional pages published, no indexing changes, no redirects, no legacy migration; `sitemap.xml`/`search-index.json`/`robots.txt` unchanged; `/guides/banking/` and `/guides/eu-registration/` return `200`. This is visual source-categorization only — not an official endorsement or certification claim. Status: **Guide source-card styling restored — final preview QA pending**. PR #21 remains unmerged.
- **Official source-link polish completed** — a final confirmation pass on PR #21's source-card work (Sprint 97F). Re-checked the implementation for rough edges: cards within the same grid row are equal height (CSS Grid's default stretch behavior, verified on `/moving-to-spain/healthcare/`'s 3-card row — all 284px), the longest category tag ("TRAFFIC AUTHORITY" on `/living-in-spain/driving/`) doesn't wrap or overflow at 390px, and click areas/spacing remain consistent with the rest of the Guide System. No further code changes were needed — the styling was already coherent, subtle, and consistent. Re-verified the full set of fixes together: `/start-here/`'s 7 "Choose your path" CTAs remain equal (197×44px), homepage Move/Vacation/Live Explore buttons remain equal (136×44px), no visible Draft badge anywhere, source cards remain calm and source-categorized without imitating official sites. Launch safety unchanged: 5 launched pages `index, follow`, 16 drafts `noindex, nofollow`, `sitemap.xml`/`search-index.json`/`robots.txt` byte-identical to `main`, no redirects, no legacy migration. Status: **Official source-link polish completed — final preview QA pending**. PR #21 remains unmerged, still scoped to exactly: CTA alignment, normalized CTA labels, homepage CTA consistency, Draft-label removal, and official source-link visual polish.
- **PR #21 final preview QA passed (Sprint 98)** — PR #21 (commit `0bfbc9c`) checked on its Netlify deploy preview (`https://deploy-preview-21--iberigo.netlify.app`) at 1280px and 390px, covering every fix bundled in the PR. Homepage: Move/Vacation/Live Explore buttons all identical (136×44px desktop, 286×44px full-width mobile), Move to Spain links to `/start-here/`, Vacation/Live presets unchanged, no standalone Start Here block, header/search/language-switcher/footer/Donate all normal, no overflow. Guide CTA: `/start-here/`, `/moving-to-spain/non-eu-citizens/`, `/moving-to-spain/family-member-eu-citizen/`, `/moving-to-spain/work-in-spain/`, `/moving-to-spain/settling-into-spain/` all show equal-width/equal-height, bottom-aligned buttons with normalized "View roadmap"/"View guide"/"Continue" labels, no wrapped outliers, TOC/reading-time/disclaimer unaffected. Draft-label QA: zero visible badges on `/start-here/`, `/moving-to-spain/eu-citizens/`, `/moving-to-spain/work-in-spain/`, `/living-in-spain/taxes/`, `/search/`; `robots` meta confirmed still correctly controlling index status on each. Source-link QA: coherent, subtly-accented, correctly-categorized cards on all 7 checked pages (`/moving-to-spain/documents-checklist/`, `/living-in-spain/opening-a-bank-account/` [no source cards, unaffected], `/moving-to-spain/eu-registration/`, `/moving-to-spain/family-member-eu-citizen/`, `/living-in-spain/taxes/`, `/living-in-spain/driving/`, `/moving-to-spain/healthcare/`), no wrapping, no overflow. Regression QA: homepage, `/the-spain-files/`, `/support/` (Donate), `/guides/banking/`, `/guides/eu-registration/` all render normally, legacy routes return `200`. Launch safety on the live preview: all 5 launched pages `index, follow`; all 16 non-selected drafts `noindex, nofollow`; preview's `sitemap.xml` byte-identical to `main` and contains only the 5 launched routes; `search-index.json` exactly 5 entries; `robots.txt` byte-identical to `main`; no redirects; no legacy migration. No issues found, no fixes needed. Status: **PR #21 final preview QA passed — merge pending**. PR #21 remains unmerged.
- **PR #21 visual fixes launched (Sprint 99, 2026-07-04)** — PR #21 squash-merged into `main` (commit `1c5b61c`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification on `https://iberigo.eu` confirmed: homepage returns `200` with Move/Vacation/Live Explore buttons visually consistent, Move to Spain linking to `/start-here/`, no standalone Start Here block; `/start-here/` returns `200`, shows 7× "View roadmap" CTAs with no visible Draft badge; official source-link cards on `/moving-to-spain/documents-checklist/` show correct category accents (police/government); all 5 launched pages remain `index, follow`; all 16 non-selected drafts remain `noindex, nofollow`; production `sitemap.xml`/`robots.txt` byte-identical to `main`, `sitemap.xml` contains only the 5 launched routes; production `search-index.json` has exactly 5 entries; no redirects; no legacy migration; `/guides/banking/`, `/guides/eu-registration/`, `/the-spain-files/`, and `/support/` all return `200` / render normally. Status: **PR #21 visual fixes launched**.
- **Visual Coherence Phase 1 planning completed (Sprint 101)** — created `docs/VISUAL_COHERENCE_PHASE1_PLAN.md`, a practical Phase 1 implementation plan following directly from the merged `docs/SITEWIDE_VISUAL_COHERENCE_AUDIT.md`. Scope: a small, additive "homepage card coherence pass" (radius/spacing/CTA-rhythm alignment on `index.html`'s situation cards only), explicitly excluding legacy `/guides/*` restructuring, redirects, additional publication, and any `sitemap.xml`/`search-index.json`/`robots.txt` change. Includes a file-risk map (`index.html` low risk; `styles.css` medium–high risk if shared selectors are touched, mitigated by preferring additive custom properties/classes; `app.js` explicitly out of scope and high-risk if touched, since it drives all 37 legacy guide pages), a recommended first PR ("Homepage card coherence pass"), a QA checklist, and a rollback plan. This is planning only — no visual implementation changes, no `styles.css` edits, no indexing changes, no redirects, no legacy migration. Status: **Visual Coherence Phase 1 planned — implementation pending**. No PR opened, no merge performed.
- **Homepage card coherence pass prepared (Sprint 103)** — implemented the Phase 1 plan's recommended first PR on `visual-coherence/homepage-card-pass`: a narrowly-scoped `styles.css` change to `.situation-card--illustrated` (border-radius 8px → 16px, matching Guide System v1 exactly; border/shadow tokens reused from Guide System v1's `.guide-info-card`) and its CTA rule (`font-weight` 600 → 800 for presence). No shared selector used by other pages (`.situation-card`, `.popular-card`, `.panel`, `.topbar`, `.site-footer`, etc.) was touched — only the homepage-specific `.situation-card--illustrated` and its button rule. No structural homepage redesign: copy, routes, hero structure, and navigation are all unchanged. Verified: all three Explore buttons remain equal-sized (136×44px desktop, full-width mobile); Move to Spain still links to `/start-here/`; Vacation/Live route-preset behavior unchanged; no standalone Start Here block; homepage card radius now matches `/start-here/`'s Guide System cards exactly (16px); no overflow at 1280px/390px; `/start-here/`'s CTA alignment and Draft-badge removal both still intact; `/the-spain-files/`, `/support/`, `/guides/banking/`, `/guides/eu-registration/` all render normally. No additional pages published, no indexing changes: 5 launched pages remain `index, follow`, 16 drafts remain `noindex, nofollow`, `sitemap.xml`/`search-index.json`/`robots.txt`/`index.html` all unchanged. No redirects, no legacy migration. Status: **Homepage card coherence pass prepared — preview QA pending**. Not yet merged.
- **Homepage card coherence preview QA passed (Sprint 104)** — PR #24 (commit `6a4b4ec`) checked on its Netlify deploy preview (`https://deploy-preview-24--iberigo.netlify.app`) at 1280px and 390px. Homepage returns `200`; all three cards render at 16px radius, confirmed identical to `/start-here/`'s Guide System cards; border/shadow read as calm, not heavy; CTA buttons remain equal-sized (136×44px desktop, full-width mobile) with a bolder but not aggressive weight. Move to Spain still links to `/start-here/`; Vacation/Live behavior unchanged; no standalone Start Here block; no overflow or crowding. Comparison with `/start-here/` confirms the homepage now reads as the same card family while still feeling like a homepage, not a guide-page clone. Regression QA: `/start-here/`'s CTA alignment and Draft-badge removal both still hold; `/the-spain-files/` and `/support/` render normally; `/guides/banking/` and `/guides/eu-registration/` both return `200`. Launch safety verified directly against the live preview: all 5 launched pages `index, follow`; all 16 non-selected drafts `noindex, nofollow`; preview's `sitemap.xml` byte-identical to `main`; `search-index.json` exactly 5 entries; `robots.txt` byte-identical to `main`; no redirects; no legacy migration; no additional pages published. No issues found, no fixes needed. Status: **Homepage card coherence preview QA passed — merge pending**. PR #24 remains unmerged.
- **Homepage card coherence pass launched (Sprint 105, 2026-07-04)** — PR #24 squash-merged into `main` (commit `4f1b088`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification on `https://iberigo.eu` confirmed: homepage returns `200` with all three cards at 16px radius (matching `/start-here/`'s Guide System cards), CTA buttons equal-sized, Move to Spain linking to `/start-here/`, Vacation/Live behavior unchanged, no standalone Start Here block; production `styles.css` byte-identical to committed `main` with both the radius and CTA font-weight changes live; `/start-here/` shows no Draft badge and CTA alignment intact; `/the-spain-files/`, `/support/`, `/guides/banking/`, `/guides/eu-registration/` all return `200`/render normally; all 5 launched pages remain `index, follow`; all 16 non-selected drafts remain `noindex, nofollow`; production `sitemap.xml`/`robots.txt` byte-identical to `main`; production `search-index.json` has exactly 5 entries; no redirects; no legacy migration; no additional pages published. Status: **Homepage card coherence pass launched**. This completes Visual Coherence Phase 1; Phase 2 (legacy `/guides/*` structural fix), Phase 3 (Vacation/Live landing pages), and Phase 4 (further homepage redesign) remain unplanned and unimplemented.
- **Visual Coherence Phase 2 legacy guide structure planned (Sprint 107, 2026-07-04)** — created `docs/VISUAL_COHERENCE_PHASE2_LEGACY_GUIDE_PLAN.md`, planning the fix for the legacy `/guides/*` structural gap identified in `docs/SITEWIDE_VISUAL_COHERENCE_AUDIT.md`. Direct inspection corrected an earlier "37 directories" estimate to the accurate figure: **35 English legacy topics** mirrored under `/guides/es/` for **70 live indexed URLs**, all still `200`, all still rendering content entirely client-side via `data-guide-id`/`app.js` with no static or `<noscript>` fallback. Plan defines four migration options (leave as-is / static wrappers / migrate into Guide System v1 / hybrid) and recommends a first PR that is a read-only validation/report script only — no page content changes — followed by a single-route static-rendering proof of concept only after that's reviewed. Planning only: no legacy guide changes, no redirects, no migration, no indexing changes, and no edits to `sitemap.xml`, `search-index.json`, `robots.txt`, `styles.css`, or `app.js` were made. Status: **Visual Coherence Phase 2 planned — implementation pending**. No PR opened, no merge performed.
- **Legacy guide audit script prepared (Sprint 109, 2026-07-04)** — built `scripts/audit-legacy-guides.js`, a read-only inspection script for legacy `/guides/*` pages, per Phase 2's recommended first implementation. Ran it and recorded findings in `docs/LEGACY_GUIDE_AUDIT_REPORT.md` and `reports/legacy-guide-audit.json`: 70 live legacy URLs (35 English, 35 Spanish), all with complete `<head>` metadata (canonical, description, hreflang, `data-guide-id`) and present in `sitemap.xml`, but 0/70 with static guide content in `<body>` and 0/70 with a `<noscript>` fallback — quantifying the structural gap first identified in the sitewide audit. No legacy guide page changed, no `app.js`/`styles.css` changes, no redirects, no migration, no indexing changes. Status: **Legacy guide audit script prepared — preview/review pending**. PR pending, not yet merged.
- **Legacy guide audit script launched (Sprint 110, 2026-07-04)** — PR #27 squash-merged into `main` (commit `c3b48b1`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification confirmed: homepage `200`, `/guides/banking/`/`/guides/es/banking/`/`/guides/eu-registration/`/`/guides/es/eu-registration/` all `200`, `/start-here/`/`/the-spain-files/`/`/support/` all `200`; all 5 launched pages remain `index, follow`; sampled drafts remain `noindex, nofollow`; `sitemap.xml`/`search-index.json`/`robots.txt`/`styles.css`/`app.js` all byte-identical to `main`. Rerunning `node scripts/audit-legacy-guides.js` on updated `main` exits `0` and reproduces identical findings (only the report's `generatedAt` timestamp differs between runs, as expected). No legacy guide HTML changed, no redirects, no migration, no additional pages published. Status: **Legacy guide audit script launched**.
- **Legacy static rendering POC planned (Sprint 112, 2026-07-04)** — created `docs/LEGACY_STATIC_RENDERING_POC_PLAN.md`, planning a single-route static-rendering proof of concept per the audit script's recommendation. Evaluated three no-overlap candidates (`job-search`, `phone`, `renting-home`); recommended `/guides/job-search/` plus its Spanish mirror `/guides/es/job-search/` (rich existing bilingual content in `app.js`'s `directRoadmapFor()`, simplest content shape). Recommended implementation method: a hybrid approach (static content + existing JS enhancement kept in place, avoiding double-rendering) built via a small generator script rather than hand-copied HTML. Defined acceptance criteria, QA checklist, file-risk map, and rollback plan for the future implementation PR. Planning only: no legacy guide page, `app.js`, or `styles.css` was changed; no redirects, migration, or indexing changes were made. Status: **Legacy static rendering POC planned — implementation pending**. No PR opened, no merge performed.
- **Job-search static rendering POC implemented (Sprint 114, 2026-07-04)** — built `scripts/render-legacy-static-poc.js`, which rewrites only the `#wizardResult` placeholder in `guides/job-search/index.html` and `guides/es/job-search/index.html` with real static guide content (title, purpose paragraph, next-steps list, 8 official job-portal links, disclaimer), transcribed directly from `app.js`'s `directRoadmapFor()` job-search branch (documented with source line references in the script, since `app.js` cannot be safely `require()`-d under Node). `<head>` metadata, canonical, hreflang, and sitemap presence are unchanged on both pages; `app.js` and `styles.css` were not touched (static markup reuses existing styled classes). Corrected `scripts/audit-legacy-guides.js`'s `hasStaticGuideContent` check, which previously relied on a meta-description substring match that would have missed this content; it now detects the `#wizardResult` placeholder directly. Audit now shows 2/70 pages with static body content, 68/70 unchanged from baseline. Verified locally: both routes return `200`, JS-enabled view renders correctly with no duplication (app.js fully overwrites `#wizardResult` on load), static content confirmed present in raw HTML for both languages, regression sample (`banking`, `eu-registration` EN+ES, homepage, `/start-here/`, `/the-spain-files/`, `/support/`) all unaffected, 5 launched pages remain `index, follow`, sampled drafts remain `noindex, nofollow`, `sitemap.xml`/`search-index.json`/`robots.txt` unchanged. Status: **Job-search static rendering POC prepared — preview QA pending**. PR pending, not yet merged.
- **Job-search static rendering POC preview QA passed (Sprint 115, 2026-07-04)** — PR #30 (commit `970a330`) checked on `https://deploy-preview-30--iberigo.netlify.app`. View-source confirmed real static content (title, purpose, next-steps list, all 8 job-portal links, disclaimer) in raw HTML for both `/guides/job-search/` and `/guides/es/job-search/`; `/guides/banking/`/`/guides/es/banking/` confirmed unaffected. JS-enabled QA showed no duplication (exactly 1 title/list/disclaimer and 8 job links after `app.js` runs, console error-free, correct EN/ES rendering). `<head>` metadata, canonical, hreflang, `sitemap.xml`, `search-index.json`, `robots.txt`, `app.js`, `styles.css` all confirmed byte-identical to `main`. `node scripts/audit-legacy-guides.js` exits `0`, deterministic apart from `generatedAt`, exactly 2/70 static body content (job-search pair only). Regression sample (homepage, `/start-here/`, `/the-spain-files/`, `/support/`) all `200`, no Draft label leak. No issues found, no fixes needed. Status: **Job-search static rendering POC preview QA passed — merge pending**. PR #30 remains unmerged.
- **Job-search static rendering POC launched (Sprint 116, 2026-07-04)** — PR #30 squash-merged into `main` (commit `63afe68`) using the established branch-protection procedure (required approving reviews 1 → 0 → merge → 1). Production verification confirmed `/guides/job-search/` and `/guides/es/job-search/` return `200`, ship meaningful static guide body content in raw HTML, preserve canonical/hreflang metadata, and render with JavaScript enabled without duplicate `#wizardResult`, hero, or roadmap-list content. Regression routes (`/`, `/start-here/`, `/the-spain-files/`, `/support/`, `/guides/banking/`, `/guides/es/banking/`, `/guides/eu-registration/`, `/guides/es/eu-registration/`) all returned `200`. `node scripts/generate-guide-system.js` and `node scripts/audit-legacy-guides.js` pass on updated `main`; the audit remains exactly 2/70 static body content routes (job-search EN+ES only), with 68/70 unchanged. Five launched Group 1 pages remain `index, follow`; 17 generated draft/noindex surfaces remain `noindex, nofollow`; `sitemap.xml`/`search-index.json`/`robots.txt`/`app.js`/`styles.css` unchanged; no redirects, no full legacy migration, no additional pages published. Status: **Job-search static rendering POC launched — production verified**.
- **Legacy static rendering rollout planned (Sprint 118, 2026-07-04)** — created `docs/LEGACY_STATIC_RENDERING_ROLLOUT_PLAN.md`, a planning-only rollout strategy for the remaining 68 client-side-only legacy routes. The plan inventories all EN+ES route pairs, classifies Guide System overlap, content complexity, static-rendering fit, risk, and suggested batches; compares all-at-once, small-batch, one-pair, and tooling-first strategies; recommends the next implementation PR convert only `/guides/phone/` + `/guides/es/phone/` while evolving the renderer into a controlled allowlist-based selected-route tool; and defines QA gates/rollback. Current live baseline remains exactly 2/70 static-body-content routes (job-search EN+ES only). No implementation started: no additional static pages, no legacy guide HTML changes, no `app.js`/`styles.css` changes, no redirects, no sitemap/search-index/robots changes, no full migration, no additional publication. Status: **Legacy static rendering rollout planned — implementation pending**.
- **Phone static rendering POC prepared (Sprint 120, 2026-07-04)** — implemented the planned second controlled legacy static-rendering POC for `/guides/phone/` and `/guides/es/phone/`. `scripts/render-legacy-static-poc.js` now uses an explicit supported-guide/selected-route structure: `job-search` remains supported as the launched POC/regression route, while only `phone` is selected for this sprint. The phone static body content is generated from existing `app.js` phone-route copy plus provider labels, intros, logos, URLs, and existing `provider-link` styles. Audit now shows exactly 4/70 static-body-content routes (`job-search` EN+ES and `phone` EN+ES), with the remaining 66/70 legacy routes unchanged. No `app.js` or `styles.css` edits, no redirects, no sitemap/search-index/robots changes, no full legacy migration, no additional pages published. Status: **Phone static rendering POC prepared — preview QA pending**. PR pending, not yet merged.
- **Phone static rendering POC preview QA passed (Sprint 121, 2026-07-04)** — PR #33 checked on `https://deploy-preview-33--iberigo.netlify.app` at commit `65ac21ca9b831535cef6fa472ea18016ab0873a1`. Static HTML confirms `/guides/phone/` and `/guides/es/phone/` now contain meaningful guide body content inside `#wizardResult`; `/guides/job-search/` and `/guides/es/job-search/` remain stable; `/guides/banking/` and `/guides/es/banking/` were not accidentally static-rendered. JS-enabled QA at 1280px and 390px found no duplication, no console errors, no horizontal overflow, correct EN/ES content, expected provider/job link counts, and stable media behavior. Regression checks covered homepage, `/start-here/`, The Spain Files, support, banking, and EU registration routes; Start Here CTA alignment remains fixed. `node scripts/generate-guide-system.js` and `node scripts/audit-legacy-guides.js` pass; audit remains exactly 4/70 static-body-content routes and 0/70 noscript fallback. No `app.js`, `styles.css`, sitemap, search-index, robots, redirects, migration, or additional publication changes. Status: **Phone static rendering POC preview QA passed — merge pending**. PR #33 remains unmerged.
- **Phone static rendering POC launched (Sprint 122, 2026-07-04)** — PR #33 squash-merged into `main` with merge commit `fac61c9660dd908d41bf4ae68dfb8d83237ac1f7` using the established branch-protection procedure (required approving reviews 1 -> 0 -> merge -> 1). Production verification passed: `/guides/phone/` and `/guides/es/phone/` return `200`, include static body content in raw HTML, preserve metadata/canonical/hreflang, and render with JavaScript enabled without duplicate result cards, titles, lists, provider links, or disclaimers. `/guides/job-search/` and `/guides/es/job-search/` remain stable. Regression routes (`/`, `/start-here/`, `/the-spain-files/`, `/support/`, banking EN+ES, EU registration EN+ES) return `200`. `node scripts/generate-guide-system.js` and `node scripts/audit-legacy-guides.js` pass on updated `main`; audit reports exactly 4/70 static-body-content routes and 0/70 noscript fallback. No `app.js`, `styles.css`, sitemap, search-index, robots, redirects, full migration, or additional publication changes. Status: **Phone static rendering POC launched — production verified**.
- **Spanish Start Here localization planned (Sprint 124, 2026-07-04)** — created `docs/SPANISH_START_HERE_LOCALIZATION_PLAN.md`, a planning-only path for localizing the new Guide System entry point. The plan recommends `/es/start-here/` as the future Spanish Guide System URL pattern, starting with Spanish Start Here as an accessible draft/noindex page before considering the five launched Group 1 pages. It defines publication/indexing rules, translation/review policy, URL-structure tradeoffs, file-risk map, first implementation PR scope, and QA checklist. No Spanish page was created, no translation was started, no `app.js`/`styles.css`/sitemap/search-index/robots/redirect change was made, no legacy guide was migrated, and no additional page was published. Status: **Spanish Start Here localization planned — implementation pending**.
- **Spanish Start Here draft prepared (Sprint 126, 2026-07-05)** — created `/es/start-here/` as the first Spanish Guide System draft page, generated from the Guide System with Spanish content, `<html lang="es">`, canonical `https://iberigo.eu/es/start-here/`, and `noindex, nofollow`. The page links only to existing English routes while Spanish guide counterparts remain uncreated. It is not in `sitemap.xml` or `search-index.json`; English `/start-here/` remains `index, follow`; no redirects, no legacy migration, no other Spanish Guide System pages, no `app.js` change, and no `styles.css` change. Status: **Spanish Start Here draft prepared — preview QA pending**.
- **Spanish Start Here draft preview QA passed (Sprint 127, 2026-07-05)** — PR #36 preview checked at `https://deploy-preview-36--iberigo.netlify.app/es/start-here/`: route returns `200`, uses `<html lang="es">`, Spanish title/meta/canonical, and remains `noindex, nofollow` with Netlify `x-robots-tag: noindex`. `/es/start-here/` is absent from `sitemap.xml` and `search-index.json`; `robots.txt` is unchanged. Desktop and mobile QA found no overflow, readable TOC behavior, 44px+ tap targets, calm disclaimer/scope blocks, and no visible Draft badge. Spanish wording is practical and cautious; English launched pages and representative legacy routes still return `200`; English `/start-here/` remains `index, follow`; generator and legacy audit checks pass with the legacy audit baseline still exactly 4/70 static-body-content routes. No additional Spanish Guide System pages, redirects, legacy migrations, publication/indexing changes, sitemap/search-index/robots changes, `app.js` changes, or `styles.css` changes were made. Status: **Spanish Start Here draft preview QA passed — merge pending**. PR #36 remains unmerged.
- **Spanish Start Here draft created (Sprint 128, 2026-07-05)** — PR #36 squash-merged into `main` (commit `7ea6b12`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification confirmed: `/es/start-here/` returns `200`, `<html lang="es">`, Spanish title/meta description, canonical `https://iberigo.eu/es/start-here/`, `robots` meta exactly `noindex, nofollow`, absent from `sitemap.xml`/`search-index.json`, `robots.txt` unchanged; desktop/mobile layout clean with no overflow and no visible Draft badge. English `/start-here/` and all 5 launched pages remain `index, follow`; the 17 existing draft/noindex surfaces are unchanged, with `/es/start-here/` as the 18th (one new); `app.js`/`styles.css` unchanged; no legacy guide HTML changed; legacy audit remains exactly 4/70 static-body-content routes. Regression sample (homepage, `/the-spain-files/`, `/support/`, `/guides/job-search/`, `/guides/es/job-search/`, `/guides/phone/`, `/guides/es/phone/`, `/guides/banking/`, `/guides/es/banking/`) all `200`. No Spanish launch happened, no additional Spanish Guide System pages created, no indexing changes made. Status: **Spanish Start Here draft created**.
- **Spanish Start Here review and launch plan created (Sprint 130, 2026-07-05)** — created `docs/SPANISH_START_HERE_REVIEW_AND_LAUNCH_PLAN.md`, planning the launch decision for `/es/start-here/`. Compared 4 launch options; recommended keeping `/es/start-here/` `noindex, nofollow` for now and creating Spanish drafts for the 5 Group 1 pages before any Spanish launch, rather than launching it alone or exposing it via the language switcher while unfinished. Defined a Spanish content review checklist (fluency, terminology consistency for terms like `empadronamiento`/`NIE`/`TIE`, no overstated legal/timeline certainty), a hreflang strategy (none added until launch-ready, then reciprocal pairs only — confirmed neither `/start-here/` nor `/es/start-here/` currently has any hreflang tags, since the Guide System generator has no hreflang logic at all yet), and a language-switcher strategy (show Spanish only where a reviewed, launch-ready counterpart exists — confirmed the current sitewide EN/ES switcher is a same-page text toggle that does not cross-navigate between `/start-here/` and `/es/start-here/` today). Recommended next implementation PR (not built in this sprint): a Spanish draft for one linked Group 1 page, likely `/es/moving-to-spain/documents-checklist/`, `noindex, nofollow`, no hreflang, no switcher change. Planning only: no launch, no sitemap/search-index change, no hreflang added, no language-switcher change, no additional Spanish page, no `app.js`/`styles.css` edit. Status: **Spanish Start Here review planned — launch pending**. No PR opened, no merge performed.
- **Spanish documents checklist draft prepared (Sprint 132, 2026-07-05)** — created `/es/moving-to-spain/documents-checklist/`, the recommended next Spanish draft, mirroring the English page's structure and content in careful, calm Spanish (no overstated legal/timeline/province certainty; consistent terminology for `empadronamiento`, `NIE`, `TIE`, `certificado digital`, `Seguridad Social`, `tarjeta sanitaria`, `cuenta bancaria`). It is `noindex, nofollow`, absent from `sitemap.xml`/`search-index.json`, has no hreflang, and has no public language-switcher launch behavior. Links only to existing routes (`/es/start-here/` and several English Guide System pages, clearly labeled "(en inglés)" where relevant); no placeholder Spanish links, no redirects. Used `showContinueJourney: false` and directly-parameterized components (`GuideSection`/`ChecklistBox`/`InfoBox`/`Cards`) to avoid the generator's non-language-aware English chrome (`QuickAnswer`/`AtAGlance`/`TipBox`/`WarningBox`/`CommonMistakes`/`RealQuestions`/`ContinueJourney` headings) — matching the convention established by `/es/start-here/`. Confirmed pre-existing English chrome ("Official Sources" heading, "Source status" note, "Last reviewed" line) is a sitewide limitation not introduced by this page. Visual QA at 1280px/390px found no overflow, clean checklist/card/warning-box rendering, and no visible Draft badge. Only `scripts/generate-guide-system.js` was changed; `app.js` and `styles.css` were not touched. English `/moving-to-spain/documents-checklist/`, English `/start-here/`, all 5 launched pages, and `/es/start-here/` all unaffected; legacy audit remains exactly 4/70 static-body-content routes. Status: **Spanish documents checklist draft prepared — preview QA pending**. Not yet merged.
- **Spanish documents checklist draft preview QA passed (Sprint 133, 2026-07-05)** — PR #39 (commit `52b3ebf`) checked on `https://deploy-preview-39--iberigo.netlify.app/es/moving-to-spain/documents-checklist/`: route returns `200`, `<html lang="es">`, correct Spanish title/description/canonical, `robots` meta exactly `noindex, nofollow`, `x-robots-tag: noindex`, absent from `sitemap.xml`/`search-index.json`. Terminology checked and confirmed consistent; all 6 English-only links labeled "(en inglés)"; no links to missing Spanish pages; no exact-timeline claims. Desktop (1280px) and mobile (390px) show no horizontal overflow, clean rendering, no visible Draft badge. `/es/start-here/` regression clean; English `/moving-to-spain/documents-checklist/`, English `/start-here/`, and all 5 launched pages remain `index, follow` and visually unaffected. Legacy/regression sample all `200`; legacy audit remains exactly 4/70. `scripts/generate-guide-system.js` diff confirmed purely additive (0 lines removed). Draft/noindex surface count now 19 (18 existing + 1 new); `sitemap.xml`/`search-index.json`/`robots.txt`/`app.js`/`styles.css` all unchanged. Known, documented limitation: "Official Sources," "Source status," and "Last reviewed" remain in English because those components aren't language-aware anywhere yet (pre-existing, sitewide, also present on `/es/start-here/`) — not a blocker for this draft. No issues found, no fixes needed. Status: **Spanish documents checklist draft preview QA passed — merge pending**. PR #39 remains unmerged.
- **Spanish documents checklist draft created (Sprint 134, 2026-07-05)** — PR #39 squash-merged into `main` (commit `ab79f31`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification confirmed: `/es/moving-to-spain/documents-checklist/` returns `200`, `<html lang="es">`, correct Spanish metadata, `robots` meta exactly `noindex, nofollow` (no contradicting `x-robots-tag`), absent from `sitemap.xml`/`search-index.json`; desktop/mobile layout clean with no overflow and no visible Draft badge; English-only links still labeled "(en inglés)". `/es/start-here/` regression clean; English `/moving-to-spain/documents-checklist/`, English `/start-here/`, and all 5 launched pages remain `index, follow`. Draft/noindex surface count confirmed at 19; `sitemap.xml`/`search-index.json`/`robots.txt`/`app.js`/`styles.css` all unchanged; legacy/regression sample all `200`; legacy audit remains exactly 4/70. No Spanish launch happened, no other Spanish Group 1 pages created, no indexing changes. Known limitation remains documented: source-card/system labels ("Official Sources," "Source status," "Last reviewed") remain English until component localization is separately planned. Status: **Spanish documents checklist draft created**.
- **Spanish finding accommodation draft prepared (Sprint 136, 2026-07-06)** — created `/es/moving-to-spain/finding-accommodation/`, the second linked Spanish Group 1 draft, mirroring the English page's structure and content in careful, calm Spanish. Terminology consistent (`contrato de alquiler`, `arrendador`, `fianza`, `agencia inmobiliaria`, `empadronamiento`, `padrón`, `NIE`, `cuenta bancaria`, `justificante de ingresos`, `suministros`; `arrendatario`/`reserva`/`seguro de hogar` not needed for this content). It is `noindex, nofollow`, absent from `sitemap.xml`/`search-index.json`, has no hreflang, and has no public language-switcher launch behavior. Links only to existing routes: `/es/start-here/`, `/es/moving-to-spain/documents-checklist/` (both Spanish, no label needed), and 4 English pages labeled "(en inglés)"; no placeholder links, no redirects. The English source page has no official-sources entry, so the Spanish page correctly has no "Official Sources" section either — sidestepping that component's known English-only heading for this page. Visual QA at 1280px/390px found no overflow, clean table/card/warning-box rendering, no visible Draft badge. Only `scripts/generate-guide-system.js` was changed; `app.js` and `styles.css` were not touched. English `/moving-to-spain/finding-accommodation/`, English `/start-here/`, all 5 launched pages, `/es/start-here/`, and `/es/moving-to-spain/documents-checklist/` all unaffected; legacy audit remains exactly 4/70 static-body-content routes. Status: **Spanish finding accommodation draft prepared — preview QA pending**. Not yet merged.
- **Spanish finding accommodation draft preview QA passed (Sprint 137, 2026-07-06)** — PR #41 (commit `a256d26`) checked on `https://deploy-preview-41--iberigo.netlify.app/es/moving-to-spain/finding-accommodation/`: route returns `200`, `<html lang="es">`, correct Spanish title/description/canonical, `robots` meta exactly `noindex, nofollow`, `x-robots-tag: noindex`, absent from `sitemap.xml`/`search-index.json`. Terminology checked and confirmed consistent (including `reserva`, used once; `arrendatario`/`seguro de hogar` correctly not needed); all 4 English-only links labeled "(en inglés)"; links to both existing Spanish drafts correctly unlabeled; no links to missing Spanish pages; no exact-timeline claims. Confirmed the missing "Official Sources" section is expected, matching the English source page. Desktop (1280px) and mobile (390px) show no horizontal overflow, clean rendering, no visible Draft badge. Both existing Spanish drafts (`/es/start-here/`, `/es/moving-to-spain/documents-checklist/`) regression clean; English `/moving-to-spain/finding-accommodation/`, English `/moving-to-spain/documents-checklist/`, English `/start-here/`, and all 5 launched pages remain `index, follow`. Legacy/regression sample all `200`; legacy audit remains exactly 4/70. `scripts/generate-guide-system.js` diff confirmed purely additive (0 lines removed). Draft/noindex surface count now 20 (19 existing + 1 new); `sitemap.xml`/`search-index.json`/`robots.txt`/`app.js`/`styles.css` all unchanged. No issues found, no fixes needed. Status: **Spanish finding accommodation draft preview QA passed — merge pending**. PR #41 remains unmerged.
- **Spanish finding accommodation draft created (Sprint 138, 2026-07-06)** — PR #41 squash-merged into `main` (commit `22f5118`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification confirmed: `/es/moving-to-spain/finding-accommodation/` returns `200`, `<html lang="es">`, correct Spanish metadata, `robots` meta exactly `noindex, nofollow` (no contradicting `x-robots-tag`), absent from `sitemap.xml`/`search-index.json`; desktop/mobile layout clean with no overflow and no visible Draft badge; English-only links still labeled "(en inglés)". Both `/es/start-here/` and `/es/moving-to-spain/documents-checklist/` regression clean; English `/moving-to-spain/finding-accommodation/`, English `/moving-to-spain/documents-checklist/`, English `/start-here/`, and all 5 launched pages remain `index, follow`. Draft/noindex surface count confirmed at 20; `sitemap.xml`/`search-index.json`/`robots.txt`/`app.js`/`styles.css` all unchanged; legacy/regression sample all `200`; legacy audit remains exactly 4/70. No Spanish launch happened, no other Spanish Group 1 pages created, no indexing changes. Status: **Spanish finding accommodation draft created**.
- **Spanish settling into Spain draft prepared (Sprint 140, 2026-07-06)** — created `/es/moving-to-spain/settling-into-spain/`, the third linked Spanish Group 1 draft, mirroring the English page's structure and content in careful, calm Spanish. Terminology consistent (`empadronamiento`, `padrón`, `NIE`, `certificado digital`, `Seguridad Social`, `cuenta bancaria`; `TIE` used once correctly in the EU-vs-TIE distinction; `tarjeta sanitaria`/`médico de cabecera`/`farmacia` not needed for this content). It is `noindex, nofollow`, absent from `sitemap.xml`/`search-index.json`, has no hreflang, and has no public language-switcher launch behavior. Links to all 3 existing Spanish drafts (no label needed) and 8 English pages labeled "(en inglés)"; no placeholder links, no redirects. Self-caught and fixed a content bug during this sprint: several inline paragraph mentions were initially written as plain text without a real `<a href>`, unlike the English original — corrected so all 8 topic mentions are genuine links. Visual QA at 1280px/390px found no overflow, clean table/card/warning-box rendering, no visible Draft badge. Only `scripts/generate-guide-system.js` was changed; `app.js` and `styles.css` were not touched. English `/moving-to-spain/settling-into-spain/`, English `/start-here/`, all 5 launched pages, and all 3 existing Spanish drafts unaffected; legacy audit remains exactly 4/70 static-body-content routes. Status: **Spanish settling into Spain draft prepared — preview QA pending**. Not yet merged.
- **Spanish settling into Spain draft preview QA passed (Sprint 141, 2026-07-06)** — PR #43 (commit `756203c`) checked on `https://deploy-preview-43--iberigo.netlify.app/es/moving-to-spain/settling-into-spain/`: route returns `200`, `<html lang="es">`, correct Spanish title/description/canonical, `robots` meta exactly `noindex, nofollow`, `x-robots-tag: noindex`, absent from `sitemap.xml`/`search-index.json`. Terminology checked and confirmed consistent; all 8 topic mentions confirmed as real links (not plain text), all labeled "(en inglés)"; links to all 3 existing Spanish drafts correctly unlabeled; no links to missing Spanish pages; no exact-timeline claims. Desktop (1280px) and mobile (390px) show no horizontal overflow, clean rendering, no visible Draft badge. All 3 existing Spanish drafts regression clean; English `/moving-to-spain/settling-into-spain/`, English `/start-here/`, and all 5 launched pages remain `index, follow`. Legacy/regression sample all `200`; legacy audit remains exactly 4/70. `scripts/generate-guide-system.js` diff confirmed purely additive (0 lines removed). Draft/noindex surface count now 21 (20 existing + 1 new); `sitemap.xml`/`search-index.json`/`robots.txt`/`app.js`/`styles.css` all unchanged. No issues found, no fixes needed. Status: **Spanish settling into Spain draft preview QA passed — merge pending**. PR #43 remains unmerged.
- **Spanish settling into Spain draft created (Sprint 142, 2026-07-06)** — PR #43 squash-merged into `main` (commit `669c35f`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification confirmed: `/es/moving-to-spain/settling-into-spain/` returns `200`, `<html lang="es">`, correct Spanish metadata, `robots` meta exactly `noindex, nofollow` (no contradicting `x-robots-tag`), absent from `sitemap.xml`/`search-index.json`; desktop/mobile layout clean with no overflow and no visible Draft badge; all 12 "(en inglés)" labels intact. All 3 existing Spanish drafts regression clean; English `/moving-to-spain/settling-into-spain/`, English `/start-here/`, and all 5 launched pages remain `index, follow`. Draft/noindex surface count confirmed at 21; `sitemap.xml`/`search-index.json`/`robots.txt`/`app.js`/`styles.css` all unchanged; legacy/regression sample all `200`; legacy audit remains exactly 4/70. No Spanish launch happened, no remaining Spanish Group 1 page created yet, no indexing changes. Status: **Spanish settling into Spain draft created**.
- **Spanish bank account draft prepared (Sprint 143, 2026-07-06)** — created `/es/living-in-spain/opening-a-bank-account/`, the fifth and final linked Spanish Group 1 draft, mirroring the English page's structure and content in careful, calm Spanish. Terminology consistent (`cuenta bancaria`, `cuenta de residente`, `cuenta de no residente`, `NIE`, `pasaporte`, `certificado de registro de ciudadano de la UE`, `nómina`, `domiciliación`, `comisiones`, `tarjeta bancaria`, `transferencia`; `Bizum` correctly not used since the English source doesn't mention it). It is `noindex, nofollow`, absent from `sitemap.xml`/`search-index.json`, has no hreflang, and has no public language-switcher launch behavior. Links to `/es/start-here/` and `/es/moving-to-spain/settling-into-spain/` (both Spanish, no label needed) and 4 English pages labeled "(en inglés)"; no placeholder links, no redirects. Visual QA at 1280px/390px found no overflow, clean table/card/warning-box rendering, no visible Draft badge. Only `scripts/generate-guide-system.js` was changed; `app.js` and `styles.css` were not touched. **All five Spanish Group 1 draft pages now exist** — all remain `noindex, nofollow`, none are in `sitemap.xml`/`search-index.json`, no Spanish launch has happened. This sprint also folded the Sprint 142 post-launch docs record (previously on unmerged branch `post-launch/spanish-settling-into-spain-draft-record`) into this same PR instead of opening a separate docs-only PR, to speed up the draft-completion phase. Legacy audit remains exactly 4/70 static-body-content routes. Status: **Spanish bank account draft prepared — preview QA pending**. Not yet merged.
- **Spanish draft set launch-readiness review prepared (Sprint 145, 2026-07-06)** — reviewed all 5 Spanish Guide System draft pages for launch readiness. Fixed stale cross-links on `/es/start-here/`, `/es/moving-to-spain/documents-checklist/`, `/es/moving-to-spain/finding-accommodation/`, and `/es/moving-to-spain/settling-into-spain/` that still pointed to English versions of pages that now have Spanish counterparts, removing the now-inapplicable "(en inglés)" labels; fixed one leftover raw-English link text ("Opening a Bank Account") on the documents-checklist page; translated that page's official-source card names/notes to Spanish. Localized `scripts/guide-components.js`'s `OfficialSources()`/`LastReviewed()` functions with a `lang` parameter (matching the existing pattern used by `ScopeNotice()`/`LegalDisclaimer()`) so Spanish pages show "Fuentes oficiales"/"Estado de la fuente"/"Última revisión" instead of the English defaults — confirmed English pages remain byte-identical. No new pages published; draft/noindex surface count unchanged at 22; all 5 Spanish pages remain `noindex, nofollow` and absent from `sitemap.xml`/`search-index.json`; all 5 English launched pages remain `index, follow`; `app.js` untouched; `styles.css` untouched; legacy audit remains exactly 4/70. Deleted the superseded `post-launch/spanish-settling-into-spain-draft-record` branch (no open PR existed for it; content already folded into merged PR #44). Status: **Spanish draft set launch-readiness review prepared — preview QA pending**. Not yet merged.
- **Spanish Guide System launch prepared (Sprint 146, 2026-07-06)** — launched all 5 Spanish Guide System pages: added to `publishedRoutes` (flips `robots` to `index, follow` and adds them to `search-index.json`, now 10 entries); manually added the 5 Spanish `<url>` entries to hand-maintained `sitemap.xml` (now 10 Guide System URLs total). Added reciprocal hreflang (`en`/`es`/`x-default`) for the 5 complete English/Spanish pairs only, via a new `launchedPair()` helper in `scripts/generate-guide-system.js`. Added a functional public language switcher for the same 10 pages only — `scripts/guide-components.js`'s `Header()` gained an `altHref` option that adds a `data-lang-href` attribute to the inactive language button, with a small inline per-page script (added to `GuideLayout`'s existing `<script>` block, not `app.js`) wiring the click; pages without a counterpart get no `altHref` and are completely unaffected (confirmed via diff). Both switcher directions tested and working. Self-caught and fixed: (1) stale "borrador"/"todavía no publicado" draft-framing language on all 5 Spanish pages' "about this page" notes, now inaccurate since launching — removed while keeping the still-true note about remaining English-only links; (2) a stray blank `<head>` line on all 21 non-paired pages from an empty hreflang array — fixed to render nothing when empty; (3) the switcher's inline script being emitted on all 26 pages instead of only the 10 paired ones — made conditional on `altHref`. Verified: all 10 paired pages return `200`, correct `index, follow`/canonical/hreflang, working switcher, no visible Draft badge; `sitemap.xml`/`search-index.json` each contain exactly the 10 Guide System URLs/entries; draft/noindex surface count decreased by exactly 5 (22 → 17); legacy audit remains exactly 4/70; `app.js` untouched; `styles.css` untouched. Status: **Spanish Guide System launch prepared — preview QA pending**. Not yet merged.
- **Spanish Guide System launched (Sprint 147, 2026-07-06)** — PR #46 squash-merged into `main` (commit `3d0e0cc`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification confirmed: all 5 Spanish pages (`/es/start-here/`, `/es/moving-to-spain/documents-checklist/`, `/es/moving-to-spain/finding-accommodation/`, `/es/moving-to-spain/settling-into-spain/`, `/es/living-in-spain/opening-a-bank-account/`) live at `200`, `index, follow`, correct canonical/reciprocal hreflang, working language switcher (tested both directions), no visible Draft/Borrador text. All 5 English paired pages remain `200`/`index, follow` with reciprocal hreflang and a working switcher to Spanish. `sitemap.xml` has exactly 10 Guide System URLs (5 EN + 5 ES) among 87 total `<loc>` entries; `search-index.json` has exactly 10 entries. Regression sample (homepage, `/the-spain-files/`, `/support/`, `/guides/job-search/`, `/guides/es/job-search/`, `/guides/phone/`, `/guides/es/phone/`, `/guides/banking/`, `/guides/es/banking/`) all `200`. `robots.txt`/`app.js`/`styles.css` all byte-identical to `main`; legacy audit remains exactly 4/70. No incomplete Spanish pages launched, no redirects, no legacy migration. Docs-only sprint — no implementation files changed. Status: **Spanish Guide System launched**. **This completes the Spanish Guide System localization and launch effort (Sprints 124–147): the full Group 1 set is now bilingual and live.**
- **Route roadmap launch-readiness review prepared (2026-07-07)** — reviewed all 14 route roadmap pages (the 7 English drafts from Sprints 46–57 plus their new Spanish counterparts from PR #50): `/moving-to-spain/{eu-citizens,non-eu-citizens,family-member-eu-citizen,work-in-spain,students,retire-in-spain,self-employed-spain}/` and `/es/moving-to-spain/...` equivalents. Content checks passed cleanly — clear and practical, no overstated legal/financial/province-specific certainty, no invented requirements, no exact timelines stated where Spain varies, EN/ES meaning aligned, natural (non-machine-translated) Spanish, correct "(en inglés)" labeling on every remaining English-only deeper link, no placeholder/broken links. Fixed one small copy gap: `/es/moving-to-spain/eu-citizens/` was missing the Spanish equivalent of its English source's legal-disclaimer framing — added a matching "Nota importante" warning box, consistent with the other 6 Spanish roadmap pages. **Recommendation: NOT ready to launch.** This is a process blocker, not a content defect: this exact roadmap family already has a standing, unresolved backlog requirement (below, and repeated under "Planned") for a qualified-professional legal/immigration review pass before publication, and — unlike the original Group 1 launch, which has an explicit documented owner decision in `docs/GROUP1_PUBLICATION_DECISION.md` — no equivalent owner publication decision has been made for this family. Per this repo's own stated policy ("Publish/index pages only after explicit legal + editorial approval — never as a side effect of another change," see Planned section below), launch requires either that professional review or an explicit owner decision to proceed without it, neither of which has happened. No launch performed: all 14 pages remain `noindex, nofollow`, outside `sitemap.xml`/`search-index.json`, no hreflang, no language switcher; `/es/start-here/` unchanged, still labeling all 7 route cards "(en inglés)". Legacy audit remains exactly 4/70; `app.js`/`styles.css` unchanged.
- **Route roadmap legal/immigration review brief created (2026-07-11)** — created `docs/ROUTE_ROADMAP_LEGAL_REVIEW_BRIEF.md`, a self-contained review request for the 14 route roadmap pages (see immediately above), covering: purpose, current publication status (all 14 still `noindex, nofollow`, outside sitemap/search-index, no hreflang/switcher, `/es/start-here/` still labeling them "(en inglés)"), the full page list grouped by topic with EN/ES pairs, what a reviewer should check (overstated legal certainty, outdated terminology, incorrect route descriptions, missing caveats, province/oficina-specific claims, exact-timing claims, legal-advice-adjacent wording, Spanish translation/terminology mismatches, disclaimer sufficiency), the requested page-by-page response format (approve as-is / approve with minor changes / do not publish until corrected, with per-issue page/section/wording/severity/reason fields), the owner-decision alternative (documented the same way as `docs/GROUP1_PUBLICATION_DECISION.md` if no professional review is obtained), the current launch criteria (professional review + fixes, or explicit owner decision — neither has happened), and a technical-notes section describing what a future launch PR would need to do (index/follow, sitemap, search-index, hreflang, switcher, `/es/start-here/` link updates, selective "(en inglés)" removal), none of which was performed in this docs-only change. No page content changed, no indexing/sitemap/search-index/hreflang/switcher change, no `/es/start-here/` change, no redirects, no legacy migration, `app.js`/`styles.css` unchanged. Legacy audit remains exactly 4/70.
- **Route roadmap official-source verification prepared — owner decision pending (2026-07-11)** — created `docs/ROUTE_ROADMAP_SOURCE_VERIFICATION_MATRIX.md`. Since a qualified professional legal/immigration review was not available, performed an official-source verification pass instead: checked every major claim on all 14 route roadmap pages (route descriptions, who each route is for, caveats, documents, work-rights wording, residence/stay terminology, EU registration wording, retirement/non-lucrative wording, self-employed/autónomo wording, timeline and province/oficina-dependent language) against official sources — BOE (Ley Orgánica 4/2000, Real Decreto 1155/2024), `inclusion.gob.es`/`extranjeros.inclusion.gob.es` procedure sheets, `exteriores.gob.es` consular pages, `seg-social.es`, and `sede.policia.gob.es` — never against blogs, law-firm articles, or AI summaries. Result: 7 of 7 topics verified with no unsupported claims; 1 of 7 (Students) needed a wording update, made in this PR, reflecting a genuine, sourced change from RD 1155/2024 (in force since 2026-05-20): non-EU higher-education students now generally have a capped weekly work allowance included in their study authorization, which the page's previous vaguer wording didn't reflect — updated in both English and Spanish, kept hedged (no exact hour figure stated on the page itself). **This is not a substitute for professional legal advice, and it is not legal advice to IberiGo or its readers.** Publication still requires either a qualified professional legal/immigration review or an explicit owner publication decision (analogous to `docs/GROUP1_PUBLICATION_DECISION.md`) — neither has happened. All 14 pages remain `noindex, nofollow`, outside `sitemap.xml`/`search-index.json`, no hreflang, no language switcher; `/es/start-here/` unchanged. Legacy audit remains exactly 4/70; `app.js`/`styles.css` unchanged.
- **Route roadmap owner publication decision prepared — launch PR next (2026-07-11)** — created `docs/ROUTE_ROADMAP_PUBLICATION_DECISION.md`, following the precedent of `docs/GROUP1_PUBLICATION_DECISION.md`. Since no qualified professional legal/immigration review was available, the owner reviewed the completed official-source verification (`docs/ROUTE_ROADMAP_SOURCE_VERIFICATION_MATRIX.md`, PR #53, commit `c73b76b`) and approved preparing the route roadmap family for publication on that basis. The decision document records: the decision itself (all 14 pages); its basis (the verification matrix, the specific official sources used, cautious wording throughout, zero unsupported claims, the one corrected student-route wording issue, and existing disclaimers); its limits (not legal advice, not a professional review, requirements can change, province/oficina variation, individual cases may need professional advice); the residual risk the owner accepts; the decision's scope (only these 14 pages — not deeper linked guides, legacy `/guides/*`, future Spanish pages, or unrelated content); the conditions a future, separate launch PR must satisfy (index/follow, sitemap, search-index, reciprocal hreflang, language switcher, `/es/start-here/` link updates, selective "(en inglés)" removal, no redirects, no legacy migration); and future-maintenance triggers (regulation changes, ministry procedure-page changes, user feedback, or a professional reviewer becoming available). **No launch happened in this PR** — this is a decision record only, clearing the authorization gate `docs/ROUTE_ROADMAP_LEGAL_REVIEW_BRIEF.md` described; a separate future launch PR must still perform the technical work. All 14 pages remain `noindex, nofollow`, outside `sitemap.xml`/`search-index.json`, no hreflang, no language switcher; `/es/start-here/` unchanged, still labeling all 7 route cards "(en inglés)". Legacy audit remains exactly 4/70; `app.js`/`styles.css` unchanged.
- **Route roadmap launch prepared — preview QA pending (2026-07-11)** — launched all 14 route roadmap pages per `docs/ROUTE_ROADMAP_PUBLICATION_DECISION.md`: changed all 14 (7 English + 7 Spanish) from `noindex, nofollow` to `index, follow` via `publishedRoutes`; manually added the 14 new `<url>` entries to hand-maintained `sitemap.xml` (87 → 101 total `<loc>` entries); `search-index.json` now has 24 entries (10 existing + 14 new), generated automatically since published pages are indexed there. Added reciprocal hreflang (`en`/`es`/`x-default`) for the 7 complete pairs via 7 new `launchedPair()` calls, matching the existing pattern from the 5 originally launched pages. Added a functional language switcher for the same 7 pairs via the existing `altHref`/`data-lang-href` mechanism — no `app.js` or `styles.css` change needed, since this is purely generator-driven. Updated `/es/start-here/`'s "Elige tu ruta" cards (Ciudadanos de la UE, Ciudadanos no UE, Reagrupación familiar, Trabajar en España, Estudiantes, Me jubilo en España, Autónomos) plus the EU roadmap card in "La mayoría de las personas empieza aquí" to link to their Spanish counterparts, removing "(en inglés)" from exactly those 8 cards; verified via automated audit that every remaining deeper link to a still-English-only guide (healthcare, taxes, Social Security, digital certificate, driving, padrón, EU registration, family reunification, digital nomad) still carries its "(en inglés)" label, on both `/es/start-here/` and all 7 newly launched Spanish pages' own deeper links — zero mislabeled or missing-label instances found. All 9 other previously-launched pages (`/start-here/` and the other 4 EN/ES pairs) confirmed unaffected — zero diff. Verified language switcher navigates both directions (tested via local preview: ES→EN and EN→ES on the retire-in-spain pair), no visible Draft/Borrador text anywhere, no overflow at 375px or desktop width. No redirects, no legacy guide migration, `app.js`/`styles.css` unchanged. Legacy audit remains exactly 4/70. Status: **Route roadmap launch prepared — preview QA pending**. Not yet merged.
- **Route roadmap pages launched (2026-07-11)** — PR #55 squash-merged into `main` (commit `b01d574`); branch protection required approving reviews temporarily set 1 → 0 → 1 per the established repo procedure. Production verification confirmed: all 14 route roadmap pages (`/moving-to-spain/{eu-citizens,non-eu-citizens,family-member-eu-citizen,work-in-spain,students,retire-in-spain,self-employed-spain}/` and their `/es/...` counterparts) live at `200`, `index, follow`, with correct reciprocal hreflang (`en`/`es`/`x-default`, confirmed 3 tags on each). `sitemap.xml` has exactly 101 total `<loc>` entries; `search-index.json` has exactly 24 entries (10 originally launched + 14 new, no unrelated drafts). Language switcher confirmed working both directions on production (`data-lang-href` correctly wired on both sides of a sample pair). `/es/start-here/`'s 7 "Elige tu ruta" cards and the EU roadmap card in "La mayoría de las personas empieza aquí" confirmed pointing to their Spanish counterparts with "(en inglés)" removed from exactly those 8 cards; every remaining deeper link to a still-English-only guide confirmed still labeled "(en inglés)". This launch was preceded by official-source verification (`docs/ROUTE_ROADMAP_SOURCE_VERIFICATION_MATRIX.md`, PR #53) and an explicit owner publication decision (`docs/ROUTE_ROADMAP_PUBLICATION_DECISION.md`, PR #54) — the launch was not a side effect of another change. All 10 previously-launched pages confirmed unaffected; `robots.txt`/`app.js`/`styles.css` all confirmed byte-identical to `main`; no redirects; no legacy migration; legacy audit remains exactly 4/70. Status: **Route roadmap pages launched**. **This completes the route roadmap family launch: 7 topics (EU citizens, non-EU citizens, family member of an EU citizen, work in Spain, students, retire in Spain, self-employed/autónomo) are now fully bilingual and live, alongside the original 5-page Group 1 set.**
- **Official government link visual consistency prepared — preview QA pending (2026-07-12)** — audited official-source/government links sitewide and found three visual variants: the Guide System's `OfficialSources()` component (already a single, consistent, well-designed pattern across all 24 launched pages — confirmed zero hardcoded official URLs render outside it), the legacy `/guides/*` wizard-result `.gov-link*` system (`app.js`/`styles.css`, already mature and vetted, left untouched to avoid legacy-guide risk), and the homepage `#sources` panel's plain, unstyled pill links (`index.html`, no official-source visual signal at all — the one genuine inconsistency). Fixed by giving the homepage panel's 13 official links the same accent-color language as the Guide System's `guide-source-card` categories (police/tax/social-security/eu/government), via a small scoped `styles.css` addition and `class="source--{category}"` attributes on 5 of the 13 links (the other 8 already default to the government accent). `app.js`, `scripts/guide-components.js`, and `scripts/generate-guide-system.js` all unchanged. Confirmed: `sitemap.xml`/`search-index.json` regenerate byte-identical to `main`; `robots.txt` unchanged; all 24 launched pages remain `index, follow`; hreflang and language switcher untouched; no redirects; no legacy migration; legacy audit remains exactly 4/70. See `docs/SITEWIDE_VISUAL_COHERENCE_AUDIT.md` for the full audit record. Status: **Official government link visual consistency prepared — preview QA pending**. Not yet merged. (This PR has since merged — see the next entry.)
- **Sitewide link visual consistency prepared — preview QA pending (2026-07-12)** — a broader pass covering all link roles sitewide (navigation, inline content, CTA/button, card, official/source, language switcher, legacy guide, footer/support). Audit found one significant gap: inline content links inside Guide System paragraph text (`.guide-section p a` etc.) had no distinguishing color at all — only `a { color: inherit; }` is set site-wide with no underline reset, so these links rendered in the exact same muted ink color as surrounding text across all 33+ generated pages, with no hover or focus-visible state. Fixed in `scripts/guide-components.js`'s `guideCss()` by giving inline content links the same brand accent (`#a64a36`) already used by buttons, breadcrumbs, and the TOC active state, plus hover/focus-visible states matching `.guide-button`'s existing pattern; explicitly reasserted that card-title links (`.guide-info-card h3 a`) stay `color: inherit` so they're unaffected. Also closed three smaller focus/hover gaps in `styles.css`: `.source-list a` (homepage official-source pills), `.site-footer a`, and `.language-switcher button` all previously had no `:focus-visible` state. Nav links, CTA/card buttons, and the legacy `.province-links`/`.gov-link*` wizard-result system were already well-designed and needed no change — the legacy system independently converged on the same "colored left accent bar" concept used elsewhere, confirming it's already visually coherent in its own right. `app.js` and `scripts/generate-guide-system.js` unchanged; all 33 generated pages changed only because `guideCss()` is inlined per-page (regeneration side effect, not a manual edit) — confirmed via diff that zero `href` attributes changed anywhere. Confirmed: `sitemap.xml`/`search-index.json` regenerate byte-identical to `main`; `robots.txt` unchanged; all 24 launched pages remain `index, follow`; hreflang (3 tags per page) and language-switcher behavior unchanged; no redirects; no legacy migration; legacy audit remains exactly 4/70. See `docs/SITEWIDE_VISUAL_COHERENCE_AUDIT.md` for the full audit record. Status: **Sitewide link visual consistency prepared — preview QA pending**. Not yet merged. (This PR has since merged.)
- **Living in Spain full section readability pass corrected and prepared — preview QA pending (2026-07-12)** — the first version of this pass audited only the Guide System pages under `/living-in-spain/` and `/es/living-in-spain/` (6 total) and concluded no dense content or NIE block existed anywhere on the site. That conclusion was **incomplete**: it missed the homepage's separate, client-rendered "Living in Spain" flow driven by `app.js`'s `directRoadmapFor()` (reached via the homepage "Vivir en España"/"Living in Spain" card → topics menu → 15 individual topics, including NIE), which is where the dense NIE paragraph the user quoted verbatim actually lives, along with 13 other similarly dense paragraphs (padron, digital, social-security, sip-card, private-health, ehic-card, banking, renting-home, job-search, taxes, phone, vida-laboral, driving-licence-exchange; `tie` was already short and left unchanged). This correction found the real source (confirmed via `resultSectionLabel("purpose")` rendering exactly "What this is for"/"Para qué sirve", matching the quoted heading) and restructured all 14 dense topics' EN+ES `explanation` strings into short labeled sections (What it is / What it is not / When you may need it / What the office may expect / Practical note / Timing, or the Spanish equivalents) with real `<ul><li>` bullets for lists, preserving every fact and keeping the NIE same-day-assignment wording non-guaranteed. Required a small, safe rendering change in `renderResultIntro()` (`app.js`, `<p>${explanation}</p>` → `<div class="result-purpose-body">${explanation}</div>`) plus supporting spacing rules in `styles.css`, so the structured HTML can actually render — no other component touched. Also inspected `guides/nie/index.html` (same SPA shell as the homepage, inherits the fix automatically, no separate edit needed) and the two `the-spain-files/nie*` article pages (a separate content vertical, explicitly out of scope, not touched). The original 6 Guide System pages were re-confirmed unchanged (already well-structured, no dense `<p>` blocks). Visual QA done in local preview at desktop and 375px mobile widths: structured NIE and renting-home content renders cleanly, no wall-of-text, no overflow. Full corrected coverage table, source citations, and before/after example recorded in `docs/LIVING_IN_SPAIN_READABILITY_PASS.md`. Confirmed: `sitemap.xml` 101 `<loc>` entries, `search-index.json` 24 entries, legacy audit stable at exactly 4/70, all 24 launched pages remain `index, follow`, no `href`/routing/indexing/hreflang/redirect/legacy-migration changes. Status: **Living in Spain full section readability pass corrected and prepared — preview QA pending**. Not yet merged.
- **Live in Spain official-link styling aligned — preview QA pending (2026-07-12)** — targeted the client-rendered `app.js` result system used by homepage → Living/Vivir topics and direct `/guides/*` SPA shells. Root cause: the existing `.gov-link` official-source card style already matched the Move to Spain result pattern, but some Live in Spain official links bypassed it because branded `jobs-*` branches rendered before official classification, and regional health-card domains were not in the official-domain list. Fixed by classifying official URLs before branded card branches and adding only missing Live in Spain official metadata/domains for SEPE/Empléate/EURES, Agencia Tributaria, regional health-card pages, and existing health/Social Security/tax links. Private banks, telecom providers, rentals, insurers, and other non-official links keep their existing branded styles. `app.js` changed; `styles.css` unchanged. Confirmed: no URL destinations changed, no routes, redirects, sitemap, search-index, robots, hreflang, language switcher, Guide System launch state, or legacy guide HTML changed; `sitemap.xml` remains 101 `<loc>` entries, `search-index.json` remains 24 entries, all 24 launched Guide System pages remain `index, follow`, noindex surfaces remain `noindex, nofollow`, and legacy audit remains exactly 4/70. Status: **Live in Spain official-link styling aligned — preview QA pending**. Not yet merged.
- **Live in Spain official source-card parity prepared — preview QA pending (2026-07-12)** — corrective PR after PR #60: classification was fixed, but the visual result still used the older wide `.gov-link` pill/card pattern rather than the Guide System / Move to Spain official-source card pattern. Updated `app.js` so official Live in Spain links render with the Guide System source-card hierarchy (`guide-source-card`, `guide-source-head`, `guide-source-badge`, `guide-source-tag`, title, description) and added a scoped `styles.css` compatibility block under `.route-links-note .province-links` matching the Guide System card grid visual language: white card, 16px radius, subtle border/shadow, colored left border, small category badge, uppercase category label, title arrow, description text, hover/focus states, and responsive grid behavior. Private/commercial links remain on their existing branded styles. Confirmed: no URL destinations changed, no routes, redirects, sitemap, search-index, robots, hreflang, language switcher, Guide System launch state, or legacy guide HTML changed; `sitemap.xml` remains 101 `<loc>` entries, `search-index.json` remains 24 entries, all 24 launched Guide System pages remain `index, follow`, noindex surfaces remain `noindex, nofollow`, and legacy audit remains exactly 4/70. Status: **Live in Spain official source-card parity prepared — preview QA pending**. Not yet merged.
- Legal/tax/immigration professional review of the core guides — editorial pass completed on **all 14 pages** as of Sprint 39; official source coverage added for all 7 High-priority pages as of Sprint 43 (see `docs/SOURCE_VERIFICATION_MATRIX.md`); human/professional legal/tax verification is the only outstanding item on every page, and is the actual publish blocker (not a code or content-structure issue)
- Documents Checklist source gaps fully closed (Sprints 62–63): Modelo 790-012 and apostille/legalisation both directly verified; EX-18 links to the general Migraciones portal (exact form page couldn't be independently confirmed, documented rather than guessed). No source gaps remain open on this page.
- **Non-EU Roadmap route-card batch complete in draft form (Sprint 57).** All 7 of the Non-EU Citizen Roadmap's route cards now have a drafted sub-guide: Family Member of an EU Citizen (Sprint 49), Student (Sprint 52), Work in Spain (Sprint 53), Retiring in Spain (Sprint 54), Family Reunification (Sprint 55), Digital Nomad (Sprint 56), and Self-Employed / Autónomo (Sprint 57). None are published — all remain `noindex,nofollow` pending legal/tax/immigration review.
- Non-EU Citizen Roadmap drafted (Sprint 46, `/moving-to-spain/non-eu-citizens/`) — needs its own editorial + legal/immigration review pass before publication
- Family Member of an EU Citizen Roadmap drafted (Sprint 49, `/moving-to-spain/family-member-eu-citizen/`) — needs its own editorial + legal/immigration review pass before publication
- Student Roadmap drafted (Sprint 52, `/moving-to-spain/students/`) — needs its own editorial + legal/immigration review pass before publication
- Work in Spain Roadmap drafted (Sprint 53, `/moving-to-spain/work-in-spain/`) — needs its own editorial + legal/immigration review pass before publication
- Retiring in Spain Roadmap drafted (Sprint 54, `/moving-to-spain/retire-in-spain/`) — needs its own editorial + legal/tax/immigration review pass before publication
- Family Reunification Roadmap drafted (Sprint 55, `/moving-to-spain/family-reunification/`) — needs its own editorial + legal/immigration review pass before publication
- Digital Nomad Roadmap drafted (Sprint 56, `/moving-to-spain/digital-nomad-spain/`) — needs its own editorial + legal/tax/immigration review pass before publication
- Self-Employed / Autónomo Roadmap drafted (Sprint 57, `/moving-to-spain/self-employed-spain/`) — needs its own editorial + legal/tax/immigration review pass before publication
- **Workflow note (Sprint 51):** no longer opening/merging a preview PR after every single draft page — draft content is now batched on `content/non-eu-route-drafts` before a PR is opened

## Planned

- Legal/editorial review of the high-risk guides (EU Registration, Healthcare, Padrón, Taxes, Social Security, Driving Licence, Non-EU Citizen Roadmap, Family Member of an EU Citizen Roadmap, Student Roadmap, Work in Spain Roadmap, Retiring in Spain Roadmap, Family Reunification Roadmap, Digital Nomad Roadmap, Self-Employed Roadmap) by a qualified professional
- Legacy guide migration — decide keep/migrate/redirect/rewrite for overlapping `/guides/` pages vs. the new core structure (see `docs/MIGRATION_PLAN.md`)
- Redirect strategy for overlapping legacy/new pages, including `/guides/eu-registration/` vs. `/moving-to-spain/eu-registration/` — design only after migration decisions are made; do not execute until approved
- hreflang strategy for any redirect — preserve the existing en/es pairing on `/guides/eu-registration/` ↔ `/guides/es/eu-registration/`
- Decide whether `/moving-to-spain/eu-registration/` replaces `/guides/eu-registration/`, or whether both stay
- Publish/index pages only after explicit legal + editorial approval — never as a side effect of another change
- Spanish-language content for the new core guides (currently English-only; legacy `/guides/es/*` pages are unaffected)
- Decide whether Start Here's "joining family" card should eventually point to a persona-chooser between the EU family member and family reunification routes, instead of defaulting to the EU family member roadmap
- Decide whether Start Here needs a dedicated remote-work/digital-nomad card, since one doesn't exist yet and the Digital Nomad roadmap is currently only linked from the Non-EU Roadmap
- Batch consistency audit passed clean (Sprint 58, `docs/NON_EU_BATCH_AUDIT.md`) — next: open the PR for the full `content/non-eu-route-drafts` batch, and plan the legal/tax/immigration review sequence across all 7 new roadmaps
- Dedicated TIE guide
- Car import guide
- Renting Guide

## Icebox

- Planner
- AI assistant
- Mobile app
- Spanish Latin America expansion

## Done

- Official source verification — "Ministry responsible for immigration" link verified and linked on all 3 affected pages (Sprint 36)
- Start Here
- EU Citizen Roadmap
- Settling Into Spain
- Documents Checklist
- Finding Accommodation Guide
- Registering on the Padrón
- Healthcare Guide
- EU Registration Guide (draft, now at `/moving-to-spain/eu-registration/` — moved off the live `/guides/eu-registration/` URL in Sprint 34)
- Bank Account Guide
- Digital Certificate Guide
- Social Security Guide
- Taxes Guide
- Driving Licence Guide
- Editorial pass (content-level, non-legal) on all 14 draft guide pages — completed Sprint 39
- PR #5 merged into `main` as an internal draft foundation (all pages remain `noindex,nofollow`) — Sprint 38
- Official source coverage added — human/professional verification still required: Taxes (Agencia Tributaria), Social Security (Seguridad Social), Driving Licence (DGT) — Sprint 43
- Non-EU Citizen Roadmap draft created, `/moving-to-spain/non-eu-citizens/`, `noindex,nofollow`, Start Here's non-EU card now links to it — Sprint 46
- Family Member of an EU Citizen Roadmap draft created, `/moving-to-spain/family-member-eu-citizen/`, `noindex,nofollow` — Start Here's "joining family" card and the Non-EU Roadmap's "Family member of an EU citizen" card both now link to it — Sprint 49
- Student Roadmap draft created, `/moving-to-spain/students/`, `noindex,nofollow` — Start Here's "moving to study" card and the Non-EU Roadmap's "Study in Spain" card both now link to it — Sprint 52
- Work in Spain Roadmap draft created, `/moving-to-spain/work-in-spain/`, `noindex,nofollow` — Start Here's "moving for work" card and the Non-EU Roadmap's "Work in Spain" card both now link to it — Sprint 53
- Retiring in Spain Roadmap draft created, `/moving-to-spain/retire-in-spain/`, `noindex,nofollow` — Start Here's "retiring" card and the Non-EU Roadmap's "Retire or live from sufficient resources" card both now link to it — Sprint 54
- Family Reunification Roadmap draft created, `/moving-to-spain/family-reunification/`, `noindex,nofollow` — includes a comparison table distinguishing it from the Family Member of an EU Citizen route; the Non-EU Roadmap's "Join family in Spain" card now links to it (Start Here's "joining family" card intentionally left pointing to the EU family member roadmap) — Sprint 55
- Digital Nomad Roadmap draft created, `/moving-to-spain/digital-nomad-spain/`, `noindex,nofollow` — includes a comparison table distinguishing it from the Work in Spain and Self-Employed routes; the Non-EU Roadmap's "Digital nomad / remote work" card now links to it (Start Here has no dedicated remote-work card, so it was intentionally left unchanged) — Sprint 56
- Self-Employed / Autónomo Roadmap draft created, `/moving-to-spain/self-employed-spain/`, `noindex,nofollow` — Start Here's "self-employed" card, the Non-EU Roadmap's "Self-employed / business activity" card, and the Digital Nomad Roadmap's comparison-table reference all now link to it. This completes drafts for all 7 of the Non-EU Roadmap's route cards — Sprint 57
- Non-EU route batch consistency audit (routes/nav, terminology, risk language, duplication, source coverage) — clean, no content fixes needed; see `docs/NON_EU_BATCH_AUDIT.md` — Sprint 58
- Non-EU route batch merged into `main` via PR #11 (commit `f365a7e`) — all 8 pages live-deployed, still `noindex,nofollow` — Sprint 60
- Publish readiness audit across all 22 draft pages — no page cleared to publish; every page still needs human legal/tax/immigration sign-off; see `docs/PUBLISH_READINESS_AUDIT.md` for per-page status, launch grouping, and recommended review sequencing — Sprint 61
- IberiGo Guide System v1 defined (calm, practical, government-adjacent, card-based, mobile-first) — answers the Sprint 65 reviewer question; see `docs/IBERIGO_GUIDE_SYSTEM_V1.md` — Sprint 66
- Guide System v1 CSS scope audit completed — draft pages are styled by a per-page inline `guideCss()` block that live pages never load, so v1 styling is safe by construction; the only shared guide-prefixed class is `.guide-card-panel` (styles.css), which is on the do-not-touch list along with all site chrome; see `docs/GUIDE_SYSTEM_V1_CSS_SCOPE_PLAN.md` — Sprint 67
